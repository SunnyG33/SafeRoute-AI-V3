"use client"

// This optional bridge is here in case other parts of the app want to import providers directly.
// Currently unused but kept for future expansion.
export default function AppProvidersBridge({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
